@model CarRental.WebApp.Areas.UserPanel.Models.ReservationEquipmentListItemVM

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Equpiment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EquipmentName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EquipmentName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.EquipmentPrice)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EquipmentPrice)
        </dd>
    </dl>

    @using (Html.BeginForm("Delete", "ReservationEquipment", new { area = "UserPanel", id = Model.Id, reservationId = Model.ReservationId }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" />
            @Html.ActionLink("Back to List", "Index", new { reservationId = Model.ReservationId }, new { @class = "btn btn-default" })
        </div>
    }
</div>
